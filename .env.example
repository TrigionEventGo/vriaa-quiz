# Shared secret: browser login at /admin/login and API writes (PUT /api/quiz-questions,
# POST/PATCH /api/live/sessions) accept either this header (x-quiz-admin-token) or an
# HttpOnly session cookie after successful login.
QUIZ_ADMIN_TOKEN=change-me-before-party
